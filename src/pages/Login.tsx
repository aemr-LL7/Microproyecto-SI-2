import React from 'react'
import {LoginNavBar} from './LoginNavBar';

export const Login: React.FC<object> = () => {
    return (
        <div>
            <LoginNavBar />
            <p>Nos encontramos en LOGIN - PRINCIPAL</p>
        </div>
    )
}
